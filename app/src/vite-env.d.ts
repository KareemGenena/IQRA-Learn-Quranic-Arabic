/// <reference types="vite/client" />

/**
 * When this bundle was built, stamped in by vite.config.ts and shown at the
 * foot of the home page — so "has the update reached this phone?" is answered
 * by looking at the phone.
 */
declare const __BUILD_ID__: string;
